import { Box } from "@chakra-ui/react";

import QRForm from "lib/components/qrform";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <QRForm />
    </Box>
  );
};

export default Home;
